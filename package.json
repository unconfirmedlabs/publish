{
  "name": "@unconfirmed/publish",
  "version": "0.2.0",
  "description": "Compose and publish immutable Sui Move packages, with an Onara-sponsored CLI.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "publish": "dist/cli.js"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json && chmod +x dist/cli.js",
    "check": "tsc --noEmit && bun run build && bun test",
    "test": "bun run build && bun test",
    "prepublishOnly": "bun run check"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/unconfirmedlabs/publish"
  },
  "bugs": {
    "url": "https://github.com/unconfirmedlabs/publish/issues"
  },
  "homepage": "https://github.com/unconfirmedlabs/publish#readme",
  "keywords": [
    "sui",
    "move",
    "publish",
    "immutable",
    "gas-sponsorship",
    "onara"
  ],
  "engines": {
    "node": ">=22"
  },
  "publishConfig": {
    "access": "public"
  },
  "packageManager": "bun@1.4.0",
  "peerDependencies": {
    "@mysten/sui": "2.28.0"
  },
  "devDependencies": {
    "@mysten/sui": "2.28.0",
    "@types/bun": "1.4.0",
    "@types/node": "24.13.3",
    "typescript": "5.9.3"
  }
}
